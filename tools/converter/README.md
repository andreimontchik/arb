The __Converter__ application reads __Arbitrage__ messages CSV file, separates arbitrage messages to __Swap__ messages and saves them in a CSV file in the current directory.

# Config
n/a

# Build 
`cargo build -p converter`

# Test
n/a

# Run
`. ~/work/src/research/bin/converter.local.sh <ARBITRAGE_MESSAGE_FILE>`
